# Core numerical and data stack (shared by simulations and experiments)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
torch>=2.2
matplotlib>=3.8
PyYAML>=6.0

# KRR baseline (two-step kernel ridge ES regression)
quantes
